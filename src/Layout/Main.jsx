import { Outlet } from "react-router-dom";

const Main = () => {
    return (
        <div>
            {/* Outlet */}
            <Outlet></Outlet>
            {/* Outlet end */}
        </div>
    );
};

export default Main;
